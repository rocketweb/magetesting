<? if (is_array($this->messages) && count($this->messages)): ?>
    <? foreach ($this->messages as $msg): ?>
        <div class="alert alert-success" data-dismiss="alert">
            <a class="close" href="#">&times;</a>
            <strong><?= $this->escape($this->translate($msg)); ?></strong>
        </div>
    <? endforeach; ?>
<? endif; ?>